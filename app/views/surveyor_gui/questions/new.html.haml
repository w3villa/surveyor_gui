%h1.ml-4.question_new_title_top= @title

%h3= "For Section: #{@survey_section.title}"
= link_to "Back To Survey", surveyor_gui.edit_surveyform_url(@survey)

= render 'surveyor_gui/questions/form', question: question