.modal-dialog{:role => "document"}
  .modal-content
    .modal-header
      %button.close.close-logic{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
        %span{"aria-hidden" => "true"} ×
      %h5#exampleModalLongTitle.modal-title= @title

    = simple_form_for @survey_section do |f|
      .modal-body
        - @survey_section.errors.full_messages.each do |msg|
          %p= msg
        = f.hidden_field :survey_id
        = f.hidden_field :modifiable
        = f.hidden_field :display_order
        = f.input :title , :input_html=>{:size=>'35'}

      .modal-footer
        = f.button :submit, 'Save Changes', class:"btn btn-success", data: { "ajax-target" => section_form_target_url, "ajax-handler" => form_handler, "ajax-error-handler" => "modalErrorHandler", "ajax-error-target" => "#modal", disable_with: "Please wait..." }
        %button.btn.btn-danger{"data-dismiss" => "modal", :type => "button"} Close

:css
  abbr{
    display:none
  }
  .survey_section_title label{    
    padding-right: 10px;
  }
